# 生活系统验收记录

验证日期：2026-09-05。引擎：Godot 4.7.1 stable。工程：`E:\project\farm`。

## 自动化结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 六张地图、农田格、出口、床铺、商店入口、NPC 路点 | PASS | `map_data_test.log` |
| 耕种、浇水、成熟、收获、出货 | PASS | `farm_state_test.log` |
| 主场景启动、移动、工具、过夜、地图切换 | PASS | `game_smoke_test.log` |
| 四季、天气、经济、NPC、节日、持久化与整合 | PASS：57 检查、0 失败 | `life_system_test.log` |

地图测试中的旧双地图清单与旧道路坐标已按现有导航 JSON 修正，保留路径填充断言，并增加草地、床铺与商店入口断言。

生活系统用例验证了春夏换季、跨年、年度节日重置、非季节播种拒绝且不扣种子、雨水结算时点、番茄重复采收、余额不足不扣库存、每日聊天和送礼上限、生日喜爱礼物 +180、四个节日的贡献和奖励、重复领奖拒绝、模型 JSON 往返、真实场景磁盘存档恢复、损坏主档恢复备份、朝作物转身、收获清除碰撞、NPC 整条闭环路径可行走、按实际位置互动、窗口暂停时间，以及午夜自动回家。

## 实际游戏验证

通过项目自带 `addons/godot_ai/` 连接编辑器会话 `farm@d2a0`，使用编辑器启动的独立游戏进程进行验证。

- `project_run` 确认 `helper_live=true`，不是编辑器预览。
- `editor_screenshot(source=game)` 获取 1152×648 实际游戏帧；所存截图均为 `stale_frame=false`。
- 真实输入发送 Escape 和 Tab，读取界面状态确认关闭对话并打开日历。
- 在城镇公告板的实际交互格执行 F 对应的交互路径，打开当日节日活动。
- 在居民实际移动位置附近执行交互，确认打开对白且写入当日聊天状态。
- 检视背包、秋季地表、冬季地表、节日彩旗与招牌。
- 向游戏发出窗口关闭通知，在隔离 QA 路径保存后正常停止；磁盘文件保存了设置的 `clock=800` 和 `energy=67`。
- 最终重新启动正常游戏：春 1 日、正式存档路径 `user://farm_life_v1.json`、自动保存启用；打开日历暂停时间，按 Esc 可开始游玩。
- 最终运行 `r1837207-6` 的游戏日志无错误；编辑器日志从检查起点 cursor 31 之后没有新增记录。历史记录不作为本次错误。

节日与季节截图使用隔离的运行时日期设置来检查展示，不冒充完整年度手工游玩；跨季清理及节日条件由自动化测试验证。QA 数据没有写入正常玩家存档。

## 图片

- `calendar.png`：正常开局的日历、生日与节日标记。
- `festival.png`：节日当天的城镇招牌和彩旗。
- `npc.png`：居民对话及当日友好度变化。
- `inventory.png`：种子、收获、季节和生长说明。

当前交付范围是种田、NPC 互动、日期季节与节日的可玩基础系统。完整居民剧情、住宅作息、恋爱婚姻及专属节日小游戏尚未实现。
