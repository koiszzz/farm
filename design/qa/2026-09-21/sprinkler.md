# 铜制洒水器验证

环境：Godot 4.7.1 stable，Windows D3D12，NVIDIA RTX 3050。

`sprinkler_gameplay_test.gd`共35项，0失败。覆盖耕作2级解锁、缺料制作原子性、铜矿/铁矿精确消费、背包和快捷栏、面向格安装、世界碰撞、宠物避障、四向作物自动浇水与生长、镐子拆回、体力消费、碰撞恢复、真实文件存档和非法设施状态拒绝。

`regional_world_test.gd`扩展为296项，确认版本2连续世界中的设施坐标与农田一起迁入区域地图，保存、读取、再次保存和再次读取都不会偏移或丢失。

真实渲染截图为`sprinkler/field.png`。画面确认洒水器位于田格中心，四周湿润作物可辨认，没有用地面文字显示范围。当前图形为程序绘制首版，后续仍需正式像素资产和放置范围预览。
